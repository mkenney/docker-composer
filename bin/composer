#!/usr/bin/env sh

if [ "self-update" == "$1" ]; then
    docker pull mkenney/composer
    wget -nv -O $0 https://raw.githubusercontent.com/mkenney/docker-composer/master/bin/composer
else
    docker run --rm -ti -v $(pwd):/src:rw -v $HOME/.ssh:/home/dev/.ssh:ro mkenney/composer $@
fi
